// Prompt User to type flavors they ordered
const userInputFlavors = prompt(
    "Test"
)

// Take users input and truns it in to array then to Object
const froyoObj ={...userInputFlavors.split(",")}
   
function froyoValue(){
    
}

console.table(froyoObj)